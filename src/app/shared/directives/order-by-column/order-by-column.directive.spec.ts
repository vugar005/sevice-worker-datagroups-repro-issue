import { OrderByColumnDirective } from './order-by-column.directive';

describe('OrderByColumnDirective', () => {
  it('should create an instance', () => {
    const directive = new OrderByColumnDirective();
    expect(directive).toBeTruthy();
  });
});
