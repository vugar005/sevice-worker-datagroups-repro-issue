import { ValueTranslatePipe } from './value-translate.pipe';

describe('ValueTranslatePipe', () => {
  it('create an instance', () => {
    const pipe = new ValueTranslatePipe();
    expect(pipe).toBeTruthy();
  });
});
